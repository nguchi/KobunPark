# Security Policy

KobunParkは、入力文字列やクリップボードに機密情報が含まれる可能性を前提に設計しています。

## Supported versions

公開後は、最新の`main`ブランチと最新リリースをサポート対象とします。

## Reporting a vulnerability

公開リポジトリのGitHub Private Vulnerability Reportingを有効化し、脆弱性はその非公開経路から報告してください。脆弱性、個人情報、トークン、実データを公開Issueへ投稿しないでください。

報告には、影響を受けるバージョン、再現手順、想定される影響、可能な範囲での緩和策を含めてください。再現用データには必ず架空の値を使用してください。

## Data handling

- 入力、処理結果、Undo／Redo履歴は既定で永続化しません。
- 変換処理はローカルで実行し、利用者の入力を外部サービスへ送信しません。
- クリップボードへの書き込みは明示的なコピー操作でのみ行います。
- 不具合報告に実際のクリップボード内容や機密情報を含めないでください。
